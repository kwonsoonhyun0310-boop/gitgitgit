LLM Agent Workspace

This repository contains notebooks and scripts for LLM agent experiments.

## MCP Servers

The file `mcp_servers.json` contains MCP server endpoints used by the project. Currently it includes:

- `docs-langchain`: https://docs.langchain.com/mcp

Update or extend `mcp_servers.json` to add more MCP server entries.
